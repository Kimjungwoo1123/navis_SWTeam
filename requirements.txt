# Testing/ 전체에서 쓰는 pip 설치 대상 패키지 목록.
# 설치: pip3 install -r requirements.txt
# (또는 install_requirements.sh 를 실행하면 아래 + picamera2(apt)까지 한 번에 처리됨)
numpy
pandas
matplotlib
scipy
opencv-python
pyserial
python-can

# ---- picamera2 는 여기 포함하지 않음 ----
# 카메라 하드웨어(libcamera)에 바로 붙는 패키지라 순수 pip보다 apt 패키지로 설치하는 게
# 훨씬 안정적입니다. install_requirements.sh 가 apt로 설치합니다.
#
# RPi.GPIO는 이제 이 레포 어디서도 쓰지 않아서 목록에서 뺐습니다 (구동 모터는 STM32에
# CAN(python-can)으로 명령만 보내고, 라이다는 USB 어댑터가 회전 모터 구동까지 자체 처리).
